---
interface Props {
	title: string;
	h1: boolean;
}

const { title, h1 } = Astro.props;
---

{
	(!h1) ?
		<p class="text-bold pt-6 text-center text-4xl">{title}</p> :
		<h1 class="text-bold pt-6 text-center text-4xl">{title}</h1>
}
